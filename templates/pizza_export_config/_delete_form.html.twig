<form method="post" action="{{ path('app_pizza_export_config_delete', {'id': pizza_export_config.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pizza_export_config.id) }}">
    <button class="btn">Delete</button>
</form>
